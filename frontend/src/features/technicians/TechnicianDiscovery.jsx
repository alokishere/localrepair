import { useCallback, useEffect, useState } from 'react'
import { Link, useParams, useSearchParams } from 'react-router-dom'
import api from '../../services/api'

function ErrorState({ message, onRetry }) {
  return <div className="rounded-xl border border-red-200 bg-red-50 p-6 text-red-700"><p>{message}</p><button onClick={onRetry} className="mt-4 rounded-lg border border-red-300 px-4 py-2 font-semibold hover:bg-red-100">Try again</button></div>
}

function TechnicianCard({ technician }) {
  return <article className="flex h-full flex-col rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start gap-4"><div className="flex h-14 w-14 shrink-0 items-center justify-center overflow-hidden rounded-full bg-blue-100 text-lg font-bold text-blue-700">{technician.avatar ? <img src={technician.avatar} alt="" className="h-full w-full object-cover" /> : technician.name.charAt(0)}</div><div className="min-w-0"><h2 className="truncate text-lg font-bold">{technician.name}</h2><p className="mt-1 text-sm text-slate-500">{technician.serviceArea || 'Local service area'}</p></div><span className="ml-auto shrink-0 rounded-full bg-green-100 px-2 py-1 text-xs font-bold text-green-700">Verified</span></div><div className="mt-5 flex flex-wrap gap-2 text-sm"><span className="rounded-full bg-amber-50 px-3 py-1 font-semibold text-amber-700">★ {technician.ratingAverage.toFixed(1)} ({technician.totalReviews})</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-600">{technician.completedJobs} jobs</span></div><div className="mt-4 flex flex-wrap gap-2">{technician.serviceCategories.slice(0, 3).map((category) => <span key={category.id} className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{category.name}</span>)}</div><Link to={`/technicians/${technician.id}`} className="mt-auto pt-6 font-semibold text-blue-600 hover:text-blue-700">View profile →</Link></article>
}

export function TechnicianListPage() {
  const [searchParams, setSearchParams] = useSearchParams(); const requestedCategory = searchParams.get('category') || ''; const [categories, setCategories] = useState([]); const [categoryId, setCategoryId] = useState(''); const [technicians, setTechnicians] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState('')
  const load = useCallback(async () => { setLoading(true); setError(''); try { const categoryResponse = await api.get('/categories'); const availableCategories = categoryResponse.data.data.categories; const queryCategory = categoryId || availableCategories.find((category) => category.slug === requestedCategory)?._id || ''; if (queryCategory && !categoryId) setCategoryId(queryCategory); const technicianResponse = await api.get('/technicians', { params: queryCategory ? { categoryId: queryCategory } : {} }); setCategories(availableCategories); setTechnicians(technicianResponse.data.data.technicians) } catch (requestError) { setError(requestError.response?.data?.message || 'Technicians could not be loaded') } finally { setLoading(false) } }, [categoryId, requestedCategory])
  useEffect(() => { load() }, [load])
  return <main className="mx-auto max-w-7xl px-6 py-12"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="text-sm font-bold uppercase tracking-[0.18em] text-blue-600">Technician discovery</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Find a verified local technician</h1><p className="mt-3 max-w-2xl text-slate-600">Compare ratings, experience, service categories, and local coverage before you choose.</p></div><label className="text-sm font-semibold text-slate-700">Filter by category<select value={categoryId} onChange={(event) => { const value = event.target.value; setCategoryId(value); const selected = categories.find((category) => category._id === value); setSearchParams(selected ? { category: selected.slug } : {}) }} className="mt-2 block min-w-56 rounded-xl border border-slate-300 bg-white px-3 py-3 font-normal"><option value="">All categories</option>{categories.map((category) => <option key={category._id} value={category._id}>{category.name}</option>)}</select></label></div>{loading ? <div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{[1, 2, 3].map((item) => <div key={item} className="h-64 animate-pulse rounded-xl border border-slate-200 bg-white" />)}</div> : error ? <div className="mt-10"><ErrorState message={error} onRetry={load} /></div> : technicians.length === 0 ? <div className="mt-10 rounded-xl border border-dashed border-slate-300 bg-white p-12 text-center"><h2 className="text-xl font-bold">No matching technicians yet</h2><p className="mt-2 text-slate-600">Try another category or check back soon.</p></div> : <div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{technicians.map((technician) => <TechnicianCard key={technician.id} technician={technician} />)}</div>}</main>
}

export function TechnicianProfilePage() {
  const { id } = useParams(); const [profileSearchParams] = useSearchParams(); const [technician, setTechnician] = useState(null); const [reviews, setReviews] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState('')
  const load = useCallback(async () => { setLoading(true); setError(''); try { const { data } = await api.get(`/technicians/${id}`); setTechnician(data.data.technician); setReviews(data.data.reviews || []) } catch (requestError) { setError(requestError.response?.data?.message || 'Technician profile could not be loaded') } finally { setLoading(false) } }, [id])
  useEffect(() => { load() }, [load])
  if (loading) return <main className="mx-auto max-w-4xl px-6 py-12"><div className="h-80 animate-pulse rounded-xl bg-white" /></main>
  if (error) return <main className="mx-auto max-w-4xl px-6 py-12"><ErrorState message={error} onRetry={load} /><Link to="/technicians" className="mt-6 inline-block font-semibold text-blue-600">← Back to technicians</Link></main>
  const bookingCategory = profileSearchParams.get('category') || technician.serviceCategories[0]?.slug || ''; const bookingProblem = profileSearchParams.get('problem') || ''
  return <main className="mx-auto max-w-4xl px-6 py-12"><Link to="/technicians" className="font-semibold text-blue-600">← Back to technicians</Link><section className="mt-6 rounded-xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex flex-col gap-6 sm:flex-row sm:items-start"><div className="flex h-24 w-24 shrink-0 items-center justify-center overflow-hidden rounded-full bg-blue-100 text-3xl font-bold text-blue-700">{technician.avatar ? <img src={technician.avatar} alt="" className="h-full w-full object-cover" /> : technician.name.charAt(0)}</div><div className="flex-1"><div className="flex flex-wrap items-center gap-3"><h1 className="text-3xl font-bold">{technician.name}</h1><span className="rounded-full bg-green-100 px-3 py-1 text-sm font-bold text-green-700">Verified</span></div><p className="mt-2 text-slate-600">{technician.serviceArea || 'Local service area'}</p><div className="mt-4 flex flex-wrap gap-3 text-sm"><span className="rounded-full bg-amber-50 px-3 py-1 font-semibold text-amber-700">★ {technician.ratingAverage.toFixed(1)} ({technician.totalReviews} reviews)</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-600">{technician.experienceYears || 0} years experience</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-600">{technician.completedJobs} completed jobs</span></div><Link to={`/booking/${technician.id}?category=${bookingCategory}${bookingProblem ? `&problem=${encodeURIComponent(bookingProblem)}` : ''}`} className="mt-5 inline-block rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white hover:bg-blue-700">Book this service</Link></div></div><p className="mt-8 leading-7 text-slate-600">{technician.bio || 'A verified LocalRepair technician ready to help with your appliance service needs.'}</p><div className="mt-6 grid gap-6 sm:grid-cols-2"><div><h2 className="font-bold">Services</h2><div className="mt-3 flex flex-wrap gap-2">{technician.serviceCategories.map((category) => <span key={category.id} className="rounded-full bg-blue-50 px-3 py-1 text-sm font-semibold text-blue-700">{category.name}</span>)}</div></div><div><h2 className="font-bold">Skills</h2><div className="mt-3 flex flex-wrap gap-2">{technician.skills.length ? technician.skills.map((skill) => <span key={skill} className="rounded-full bg-slate-100 px-3 py-1 text-sm text-slate-700">{skill}</span>) : <span className="text-sm text-slate-500">Profile skills coming soon</span>}</div></div></div></section><section className="mt-8"><h2 className="text-2xl font-bold">Recent reviews</h2>{reviews.length ? <div className="mt-4 space-y-4">{reviews.map((review) => <article key={review.id} className="rounded-xl border border-slate-200 bg-white p-5"><p className="font-semibold text-amber-600">{'★'.repeat(review.rating)}<span className="ml-2 text-slate-500">{review.customer?.name || 'Customer'}</span></p><p className="mt-2 text-slate-600">{review.comment || 'No written comment.'}</p></article>)}</div> : <p className="mt-4 rounded-xl border border-dashed border-slate-300 p-6 text-slate-600">No reviews yet.</p>}</section></main>
}
