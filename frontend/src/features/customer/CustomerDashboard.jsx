import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import api from "../../services/api";
import { useAuth } from "../../context/useAuth";

export default function CustomerDashboard() { const { user } = useAuth(); const [repairs, setRepairs] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { api.get("/repairs").then(({ data }) => setRepairs(data.data.repairs)).catch((e) => setError(e.response?.data?.message || "Unable to load your dashboard")).finally(() => setLoading(false)); }, []); const active = repairs.find((item) => !["COMPLETED", "CANCELLED", "REJECTED"].includes(item.status)); return <main className="mx-auto max-w-7xl px-6 py-12"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-bold uppercase tracking-[0.18em] text-blue-600">Customer workspace</p><h1 className="mt-3 text-4xl font-bold">Welcome back, {user?.name?.split(" ")[0] || "there"}</h1><p className="mt-2 text-slate-600">Track your repair and find help when you need it.</p></div><Link to="/diagnosis" className="rounded-xl bg-blue-600 px-5 py-3 text-center font-semibold text-white">Find a technician</Link></div>{error && <div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-red-700">{error}</div>}{loading ? <div className="mt-8 h-56 animate-pulse rounded-2xl bg-white" /> : <><section className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">Total bookings</p><p className="mt-2 text-3xl font-bold">{repairs.length}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">Active repair</p><p className="mt-2 text-3xl font-bold">{active ? 1 : 0}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">Completed</p><p className="mt-2 text-3xl font-bold">{repairs.filter((item) => item.status === "COMPLETED").length}</p></div></section><section className="mt-8 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-2xl font-bold">Upcoming booking</h2><Link to="/customer/repairs" className="font-semibold text-blue-600">View all</Link></div>{active ? <Link to={`/customer/repairs/${active.id}`} className="mt-5 block rounded-xl bg-blue-50 p-5"><div className="flex justify-between gap-4"><div><h3 className="font-bold">{active.title}</h3><p className="mt-1 text-sm text-slate-600">{active.technician?.name || "Technician assigned"} · {active.preferredDate?.slice(0, 10)} · {active.preferredTime}</p></div><span className="h-fit rounded-full bg-blue-100 px-3 py-1 text-xs font-bold text-blue-700">{active.status}</span></div></Link> : <div className="mt-5 rounded-xl border border-dashed border-slate-300 p-8 text-center"><h3 className="font-bold">No upcoming bookings</h3><p className="mt-2 text-slate-600">Start with a quick diagnosis and choose a local technician.</p><Link to="/diagnosis" className="mt-4 inline-block font-semibold text-blue-600">Find a technician →</Link></div>}</section><section className="mt-8"><h2 className="text-2xl font-bold">Quick actions</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><Link to="/customer/repairs" className="rounded-xl border border-slate-200 bg-white p-5 font-semibold hover:border-blue-300">My bookings <span className="block mt-1 text-sm font-normal text-slate-500">Review repair status and history</span></Link><Link to="/profile" className="rounded-xl border border-slate-200 bg-white p-5 font-semibold hover:border-blue-300">My profile <span className="block mt-1 text-sm font-normal text-slate-500">Update contact and location details</span></Link></div></section></>}</main>; }
